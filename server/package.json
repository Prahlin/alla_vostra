{
  "name": "alla-vostra-stripe-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx vercel dev",
    "test": "node --check api/payment-sheet.js && node --check api/health.js"
  },
  "dependencies": {
    "stripe": "^18.3.0"
  }
}
